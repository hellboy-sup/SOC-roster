name = "soc-roster-hub"
main = "src/index.js"
compatibility_date = "2026-07-29"
compatibility_flags = ["nodejs_compat"]

# All KV bindings, secrets, and environment variables
# are managed via the Cloudflare Dashboard.
